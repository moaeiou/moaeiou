name: Fuck Microsoft Azure!

on:
  workflow_dispatch:
  push:

permissions:
  contents: write

jobs: 
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Preparing build environment
        uses: moaeiou/moaeiou/actions/build-env@main
        
      - name: Prepare SDK
        uses: moaeiou/moaeiou/actions/prepare-sdk@main
        with:
          sdk_arch: "x86-64"
        
      - name: Fuck Microsoft Azure!
        shell: bash
        run: |
          curl ipinfo.io
          sudo apt-get update
          sudo apt-get install krita kdenlive nodejs nginx python3
