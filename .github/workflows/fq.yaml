name: Fuck Microsoft Azure!

on:
  workflow_dispatch:
  push:

permissions:
  contents: write

jobs: 
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Fuck Microsoft Azure!
        shell: bash
        run: |
          sudo apt update
          sudo apt install krita kdenlive nodejs nginx python3
      - name: Pull his underwear
        shell: bash
        run: |
          pwd
          curl ipinfo.io
